# Remediation Engine Configuration

# 32-character encryption key for database secrets (MANDATORY)
# Generate one using: openssl rand -base64 32
ENCRYPTION_KEY=

# Server Port
PORT=8080

# Security
# API Key for Admin/API access. If set, requires 'Authorization: Bearer <key>' or 'X-API-Key'
ADMIN_API_KEY=

# JWT Secret for user sessions (MANDATORY)
# Generate one using: openssl rand -base64 32
JWT_SECRET=

# CORS Allowed Origin (Default: http://localhost:5173)
ALLOWED_ORIGIN=http://localhost:5173

# --- AuthMind API Defaults ---
# These are used to seed the database on the first run.
AUTHMIND_BASE_URL=https://qa.authmind.net/amapi/v1
AUTHMIND_TOKEN=your-bearer-token-here